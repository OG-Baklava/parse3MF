[build]
  # Build the library first, then install + build the demo
  command = "npm install && npm run build && cd demo && npm install && npm run build"
  publish = "demo/dist"

# SPA fallback — serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
